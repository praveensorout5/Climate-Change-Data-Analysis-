import random

def predict_esg(company_name: str) -> float:
    """
    Mock ESG prediction function.
    Returns ESG score between 0 and 100.
    Replace this with your real AI/ML model inference.
    """
    return round(random.uniform(30, 95), 2)

